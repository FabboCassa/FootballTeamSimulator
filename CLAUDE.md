# CLAUDE.md — session context for Football Team Simulator

Read ARCHITECTURE.md (design) and ROADMAP.md (plan + current status via checkboxes) before doing anything.

## Working agreement (do not violate)
- **NEVER run `git commit` / `git add` / any stateful git command** — the user commits from his Windows machine. Concurrent git access through the synced folder corrupts the index. Touch source files only.
- One roadmap task at a time. The user tests every task on his machine (`dotnet test`, Unity) before it is marked `[x]` in ROADMAP.md. Mark `[~]` while in progress.
- The sandbox has NO dotnet SDK and cannot reach Microsoft/dot.net domains — code cannot be compiled here. Write conservative, standard C# and let the user build. If a test might be statistically fragile, say so and ask for the output.
- Balance-sensitive changes go through the 1,000-match harness; the user pastes the printed distribution and we judge together before closing the task.
- Language: chat in Italian, all code/comments/docs in English.

## Environment facts
- User machine: Windows, .NET 10 SDK, Unity 6.3 LTS (6000.3.17f1), project folder `C:\Users\Fabbo\FootballTeamSimulator`.
- Unity project = `client/` subfolder (opened via Unity Hub); git repo = monorepo root, single GitHub repo for everything.
- After ANY change in `shared/`, the user must run `.\tools\build-simcore.ps1` so Unity gets fresh DLLs (Sim.Core.dll + Fts.Contracts.dll → `client/Assets/Plugins/SimCore/`, gitignored, .meta committed).
- Test command: `dotnet test` (all) or with harness output:
  `dotnet test shared/Sim.Core.Tests/Sim.Core.Tests.csproj --logger "console;verbosity=detailed"`
- Current test count: 83 green.

## Architecture quick reference
- `shared/Sim.Core` (netstandard2.1, no Unity deps, no I/O, deterministic): ALL game rules. Namespaces: Domain, Generation, Match, Config, Random (+ empty: Market, Development, Condition, Tactics, Career).
- Determinism rules: only `Pcg32`/`IRandomSource` for randomness; no `Math.Pow/Exp/Log/Sin` (use IntPow pattern); no DateTime.Now; integer math where possible. This enables replays = (seed + inputs) and the IL2CPP cross-check (task 1.6).
- Every tunable lives in `Config/BalanceConfig.cs` (sections: Generation, Match). No magic numbers in systems.
- `server/` = ASP.NET Core skeleton (Api/Application/Infrastructure) with /health; docker-compose (postgres 17, redis 8). Real backend work starts Phase 7.
- Unity client: MVP + VContainer + UniTask + UI Toolkit, asmdefs FTS.App/Services/Presenters/Views/MatchView. Only Boot scene exists so far.
- Localization: ALL user-facing strings go through ILocalizationService (tables in client/Assets/Resources/Localization/{en,it}.json, flat key->text). Views can't reference Services, so they receive a Func<string,string> translate delegate from presenters. Missing keys fall back to en, then render the key itself. NEVER hardcode UI strings in views/presenters. Dev console logs stay English.

## Status (2026-06-12)
- Phase 0 done (0.5 CI: workflow exists, verified on first GitHub push).
- Phase 1 done. 2.1 done (app shell: MessageBroker, ScreenNavigator with per-screen child scopes + Reveal() hook, placeholder screens, AppLifetimeScope/GameSessionService/AppEntryPoint; views code-built, UXML arrives 2.4+; Boot scene has UIDocument + PanelSettings + AppLifetimeScope).
- 2.2 done (career setup & save: Newtonsoft via manifest; CareerState; CareerFactory; LocalJsonSaveRepository gzip+atomic+FNV-hash log in persistentDataPath/career.sav; CareerSetup screen; MainMenu Continue with friendly errors; user verified save/load/corruption).
- 2.3 done (calendar & advance day: Sim.Core Fixture/Season/LeagueTable/FixtureGenerator/SeasonProgressor, SeasonBalance config, save v2 + v1→v2 migration, LocalClock, Hub Advance Day + console table; 59 tests green, user verified).
- **2.4 squad screen IN PROGRESS**: code written — Sim.Core: Match/LineupPlan (serializable, From/Materialize/TryMaterialize), SeasonProgressor.AdvanceDay now takes optional per-club LineupPlan dict and returns List<MatchOutcome> (fixture+report; invalid plans fall back to BestEleven silently); 5 new/updated tests (expect 64 green). Client: CareerState.UserLineup (additive, still save v2, null = auto), LocalClock passes user plan + logs user match result/scorers/XI-source to console, SquadView+SquadScreenPresenter (slot list + roster, tap-slot-then-player with swap semantics, Auto Pick/Save; player *value* column deferred to 5.1, formations beyond 4-3-3 to 3.2). NEEDS: build-simcore.ps1 + dotnet test + Play-mode test. MatchEngine.Version = 2 since 1.5 (position stream; score/events per seed unchanged from v1).
- 2.4 done (squad screen: LineupPlan in Sim.Core, SeasonProgressor plans+MatchOutcome, SquadView/presenter, user XI used in sim; 64 tests green, user verified).
- Localization infra done early (pulled from 6.2): LocalizationService + en/it JSON tables, all existing UI strings migrated, language toggle in MainMenu (cycles en/it, persisted in PlayerPrefs, default from system language). User verified.
- 2.5 done (league screen: ScorerTally/Season.Scorers in Sim.Core, LeagueView 3 tabs table/fixtures/scorers; 65 tests green, user verified).
- 2.6 done (match day instant: UserMatchLog handoff, MatchResultScreen with event timeline, LineupStrength harness best 72 vs worst 16 pts; 66 tests green, user verified).
- 2.7 done (season rollover: two-division worlds with DivisionStrengthStep 14, SeasonRollover P/R 3 + ages+1 + new fixtures from Pcg32(seed ^ year*mix, 777+division), CareerState v3 Leagues + legacy "League" JSON adapter + v2→v3 migration generating div2, SeasonService/SeasonEndScreen, Hub Next Match/End Season; 72 tests green, user verified). CareerFactory consts Div2GenSequence 55 / FixtureSequence 777 / Div2FixtureSequence 778 / Div2FirstClubId 101 / Div2FirstPlayerId 5001 — NEVER change (migrations depend on them).
- **🏁 PHASE 2 COMPLETE — vertical slice: a full season is playable end-to-end. Phase-end audit passed (determinism greps clean, no hardcoded UI strings, en/it tables at parity incl. format placeholders, asmdef layering respected, no file >300 lines in Sim.Core, no magic numbers outside BalanceConfig).**
- 3.1 done (match renderer, client-only, NO Sim.Core change → 72 tests stay green, no build-simcore needed). MatchView/MatchRenderer (VisualElement, painter2D top-down pitch + interpolated tokens; playback in wall-clock time scaled by speed, BaseSecondsAt1x 180; trajectories pre-smoothed once with a centred moving average — PlayerSmoothingRadius 2 / BallSmoothingRadius 3 — to kill per-tick noise jitter; no per-frame allocs; events MinuteChanged/EventReached/Finished). Views/MatchWatchView (HUD score+clock, controls 1x/2x/4x/Skip/Continue, event toast). Presenters/MatchWatchScreenPresenter (user club teal vs opponent red, running score, Continue → Pop to Hub then Push MatchResult timeline). HubPresenter pushes MatchWatchScreenPresenter on user match. FTS.Presenters asmdef += FTS.MatchView. Loc keys match.clock/speed_1x/2x/4x/skip (en+it). ARCH §5.3/§5.4 updated (renderer = UI Toolkit screen in stack, not additive scene). User verified (watchable, runs). Known quirk: off-ball movement still looks wandery (stream is presentation-only geometry, not real steering) — deeper movement realism deferred to backlog.
- 3.2 done (Sim.Core tactics system, user verified 80 green): new Tactics namespace — Formation (6 shapes 4-3-3/4-4-2/3-5-2/4-2-3-1/5-3-2/3-4-3 as role lists; distinct def/mid/att buckets, wide mids = CM, wide forwards = W); TacticInstructions (Mentality/Pressing/Tempo/Width, neutral middle) + Tactic struct (value-equatable, keys familiarity); TacticModifiers.Compute → attack/mid/def multipliers (self-effects = symmetric trade-offs; counter-matrix = zero-sum-over-field patterns [press↔tempo, mentality↔tempo, cyclic width]; familiarity penalty on att+def); FamiliarityModel + TacticFamiliarityLog (in-memory, save wiring deferred to 3.3); MatchTactics/TacticContext. BalanceConfig.Tactics (magnitudes only; ±patterns are structural in code). MatchEngine.Simulate gained optional MatchTactics param — DEFAULT null = byte-identical to pre-3.2 (no Version bump; golden master + existing 72 tests unaffected; neutral full-familiarity tactic also = identity, proven by test). TeamRatings.WithMultipliers; LineupSelector.BestEleven(club, Formation) overload. Tests: TacticsTests (8) — identity/determinism, all formations valid, self-effects symmetric, counter sign + width zero-sum (deterministic, provable), familiarity-beats-fresh (match harness, safe), counter match harness (informational, ±band), no-dominant-tactic sweep (81 combos round-robin, win rate <56% target). 80 tests (72 + 8), all green on run 1 (familiarity 49 vs 24 ✅; no-dominant top 53.8% Def/High/Slow/Wide < 55% ✅). BUT counter edge fell short: fast-vs-press 49.2% of decided games (target 55-60%) — confirmed the possession-confound risk: pressing's +midfield and tempo's −midfield self-effects created a possession gap (squared by PossessionSharpness) that drowned the counter. FIX (run 2 pending): possession now moved ONLY by Width; Pressing & Tempo self-effects rerouted to attack/defense; press↔tempo counter made dual (fast side +attack through the press, presser −defense exposed); mentality↔tempo counter moved to defense. Counter sign test now checks Defense (presser) + Attack (fast side). Run 2: counter now 56.0% of decided games ✅ (in 55-60% target, CounterPressVsTempoPercent 6); no-dominant top win rate 53.8% (Def/High/Slow/Wide) < 55% ✅; familiarity unchanged ✅. One over-strict EXTRA assert tripped (worst points share 38.8% < my 0.40 floor — Att/High/Slow/Narrow, a legitimately bad tactic; the ✅ has no floor) → floor relaxed to 0.35 as a broken-tactic tripwire only. Mild accepted asymmetry: defensive tactics edge out attacking ones (defense ratings sit above attack — same known quirk GoalCoefficient compensates). Save wiring of the user's chosen tactic + per-club familiarity is deferred to 3.3 (TacticFamiliarityLog is in-memory for now). Build warning seen at run 2 (1 warning, non-blocking) — chase if it recurs.
- 3.3 done (tactics screen, user verified 83 green + Play-mode). Harness: attacking GF/GA 80/37 vs defensive 48/17 over a season (large, correctly-signed effect); neutral-full-fam = season identity; user tactic leaves AI matches byte-identical ✅. Sim.Core: TacticPlan (serializable formation+instructions, Key/ToTactic/FromTactic, mirrors LineupPlan); SeasonProgressor.AdvanceDay gained optional `IReadOnlyDictionary<int,TacticContext> tactics` (4th arg) — per-fixture BuildTactics returns null when neither side has an entry (AI-vs-AI byte-identical) else fills missing side with TacticContext.Neutral(FamiliarityMax)=identity. Client: CareerState +UserTactic (TacticPlan, null=neutral 4-3-3, no tactic passed to sim while null) +TacticFamiliarity (Dictionary<string,int> keyed by TacticPlan.Key); save v3→v4 (additive, no regen). LocalClock builds the user-club TacticContext from UserTactic+stored familiarity, passes it, and after a played user match accumulates familiarity via FamiliarityModel.Accumulate (uses default BalanceConfig().Tactics). TacticsView (formation cycle, 4 instruction cycle-buttons, familiarity %, read-only next-opponent panel name+venue+best-XI OVR + "tactics unknown") + TacticsScreenPresenter (works on a clone; Save persists UserTactic and, IF formation changed vs previously-saved formation, re-picks UserLineup=BestEleven(club,formation) so Squad screen + in-match shape match). Hub "Tactics" button (between Squad and League). Squad screen now formation-aware: BestEleven + Auto Pick use UserFormation()=UserTactic?.Formation ?? F433; squad.lineup_caption no longer hardcodes "(4-3-3)". Loc: hub.tactics + full tactics.* block (formation/instruction names, familiarity, opponent line) en+it at parity. Tests added: SeasonProgressorTacticsTests (3) — neutral-full-fam = season identity, user tactic leaves AI matches byte-identical + changes ≥1 own match, attacking-vs-defensive over a season scores+concedes more (harness w/ TestContext output, large effect size, full familiarity to isolate the trade-off). Expect 83 green (80+3); existing 80 pass null so unchanged. DESIGN NOTE for review: a freshly-picked tactic starts at familiarity 0 → −UnfamiliarPenaltyPercent (12%) on att+def until it accrues (+20/match, full after 5) — intended per 3.2 mechanic; flag if it feels punishing. New .cs files need Unity to generate .meta on first import.
- 3.4 done (in-match interaction, user verified: Sim.Core 91 green + Play-mode pause/sub/tactic re-sim). Sim.Core (tested): MatchInput/MatchPlan (kickoff input + scheduled per-minute changes); MatchEngine.Simulate(MatchPlan,rng) applies changes at minute boundaries — empty plan byte-identical to legacy (golden masters safe), prefix before a change reproduces exactly, only the remainder re-rolls, same plan replays identically; legacy Simulate(home,away,rng,tactics) delegates to a 1-input plan. Position stream uses the FINAL active lineups (cosmetic; result never affected). SeasonProgressor: public FixtureRng(worldSeed,fixtureId) (shared seed), RecordResult/RevertResult (commit/undo a report's scorers; revert removes zeroed tallies). Tests: MatchResimTests (5: empty=legacy hash, replay-identical, prefix-identical+diverges, attacking switch raises 2nd-half scoring [resim 151→187], substitution keeps prefix+valid) + SeasonProgressorResultTests (3: FixtureRng determinism, record↔revert, revert+record replace w/ tally invariant). Client (done, untested): MatchRenderer.SeekToMinute (resume from pause minute, skip already-shown toasts); UserMatchContext/Holder (Game scope) reconstructs the user match's kickoff MatchPlan EXACTLY as SeasonProgressor resolved it (same lineup fallback, tactics, FixtureRng) so the unchanged re-sim == committed result; LocalClock.BuildUserMatchContext fills it after the user match (famAtKickoff captured before familiarity accrues). MatchWatchView +Pause button; new InMatchPanel overlay (on-pitch/bench tap-to-sub like Squad, 4 instruction cycles like Tactics, subs-left, Apply/Resume). MatchWatchScreenPresenter rewritten: pause→stop renderer→panel; sub (tap pitch slot then bench player, max 5 subs, MaxSubstitutions client const) and/or change instructions (NOT formation in-match); Apply re-sims _plan.WithChange(shownMinute+1, input) via FixtureRng and rebuilds the renderer seeked to the pause minute; Continue commits ONLY if changed (RevertResult(baseline)+RecordResult(current)+update UserMatchLog+save) so an intervened result becomes official and the league table stays consistent. GameSessionService registers UserMatchContextHolder. Loc: match.pause + inmatch.* (en+it parity); reuses tactics.*/role.* for instruction/role labels. DESIGN NOTES: (a) in-match tactic familiarity is PER-TACTIC (FamFor): the kickoff tactic keeps its kickoff familiarity, any other tactic switched to mid-match uses the club's stored familiarity for that key (0 if never drilled) → a switch to a drilled alternative is safe, an improvised one costs −UnfamiliarPenalty, so a touchline change can help/hurt/do-nothing by habituation (user-requested); familiarity % shown in the in-match panel; (b) formation can't change mid-match (would need role reassignment), instructions only; (c) re-sim is full 90' each Apply (instant). FUTURE: physical condition/form/fitness will also weigh on this in Phase 4.1; possible backlog item = named tactic preset slots for quick offensive/defensive switching. New .cs files need Unity to generate .meta on first import.
- 3.5 done (pre-match plans, Sim.Core verified 101 green = 91 + 10; client wiring untested in Play-mode — inert until an editor exists, see note (c)). Harness: the 10-scenario ✅ fires exactly like a static change on every seed (minute-only 40/40; losing@1 17/40, winning@60 24/40, drawing@70 15/40, not-losing@80 23/40, not-winning@65 32/40, losing-by-2@1 8/40, winning-by-5@1 1/40 — all branches exercised); headless plan changed 15 of the club's fixtures, all other fixtures byte-identical, run deterministic; no calibration regression (2.44 g/m, golden masters intact, counter 56%, sweep top 53.8%). Scope agreed with user: Sim.Core foundation + headless execution + minimal save plumbing, NO editor screen yet; triggers are minute+scoreline only (fitness "tired→sub" hooks in at 4.1). Sim.Core: Match/MatchRule.cs — `ScoreSituation` (Always/Losing/Drawing/Winning/NotWinning/NotLosing, Margin≥1 on Losing/Winning), `Substitution` (OutPlayerId + already-resolved In Player, keeps engine Club-free), `RuleAction` (optional TacticInstructions+InstructionFamiliarity and/or Sub; IsEmpty), `MatchRule` (FromMinute/When/Margin/Action, side-agnostic, ConditionMet(own,opp)), `MatchRuleApplier.Apply(active,rule,isHome,famMax)` (pure: sub rebuilds the side's lineup keeping the out-slot role; instruction change rebuilds the side's TacticContext keeping formation, other side untouched). MatchEngine: new `Simulate(plan, homeRules, awayRules, rng)`; legacy `Simulate(plan,rng)` delegates with null,null. Rules evaluated at each minute boundary AFTER static plan.Changes, BEFORE the action draw, against score-so-far; fire-once via local bool[] (no hidden state → pure (plan,rules,seed)); a fired rule that yields an invalid input is skipped but marked spent. NO RNG consumed by evaluation → null/empty/never-fire = byte-identical (golden masters safe, no Version bump). Match/PrematchPlan.cs: serializable `PrematchRule`/`PrematchPlan` (ids+enums like LineupPlan/TacticPlan) + `Resolve(club, Func<TacticInstructions,int> familiarityFor, famMax)` → List<MatchRule> (drops empty actions + subs whose In player isn't in the squad). SeasonProgressor.AdvanceDay gained optional 6th arg `IReadOnlyDictionary<int,IReadOnlyList<MatchRule>> rules`; per fixture, a side with rules routes through the rules overload, neither-side-has-rules keeps the exact legacy call (AI-vs-AI byte-identical) — this IS the skipped/unwatched execution path. Client: CareerState +UserPlan (PrematchPlan, null/empty=none) save v4→v5 (additive, no migration); LocalClock.ResolveUserRules (familiarity per resulting formation+instructions via TacticPlan.Key) passes the user club's rules into AdvanceDay AND into BuildUserMatchContext; UserMatchContext +HomeRules/AwayRules (user side only) so the 3.4 watch re-sim reproduces the committed result; MatchWatchScreenPresenter.OnApply now re-sims via the rules overload (null,null when no plan = byte-identical to 3.4). Tests: PrematchPlanTests (8: null-rules=legacy, empty-lists=legacy, never-fire=baseline×50 seeds, replay-identical, prefix-preserved, **TenScenarios_FireExactlyLikeAStaticChange** = the 10-scenario ✅ proved by equivalence to a static MatchInputChange at the first gate-open minute, Resolve keeps-valid/drops-invalid, resolved-rule fires like static) + SeasonProgressorPlanTests (2: headless plan changes own matches + leaves others byte-identical over a season, headless run deterministic). DESIGN NOTES for review: (a) rules fire at most once (oscillation-free; author two rules for "attack when behind / defend when ahead"); (b) instruction-rule familiarity is host-resolved per tactic (default famMax in harness; client reads TacticFamiliarity, 0 if never drilled → −UnfamiliarPenalty); (c) NO plan editor UI yet — UserPlan is currently only settable in code/save, the screen is a later step; (d) the user-match watch path stays consistent because rules live on the user's side of UserMatchContext and the re-sim overload is byte-identical with no plan. New .cs files (MatchRule.cs, PrematchPlan.cs) need Unity to generate .meta on first import.
- **4.1 IN PROGRESS (condition model, Sim.Core only — 111 green verified by user; SeasonProgressor/client wiring still TODO).** Verified harness numbers: form 3-season longest run below 35 = **4** matches (≤6 ✅); fitness→result fresh 517 vs exhausted 318 goals/400 matches ✅; rotation vs fixed XI = fixed 72 pts vs rotated 89 pts over 6×16 congested matches (clear ~24% edge, robust). Pre-existing calibration UNTOUCHED (golden hash 0xCDEA5A2F7B9E5CF6, 2.44 g/m, draws 24.8%, strong 82%, counter 56%, sweep top 53.8%) — proves the opt-in flag default-off is byte-identical. NOTE: first rotation harness (single 14-match block, gap 2d) passed by only 15-vs-14 pts (fragile coin-flip) → hardened to **6 blocks × 16 matches, gap 1 day (truly congested), aggregated points** so the sign is robust; fixedEndFitness sanity tightened to <60. New `Condition` namespace: `Condition/ConditionModel.cs` — pure/deterministic, integer math + one seeded RNG draw for the form walk, no transcendental fns. `PerformanceMultiplier(condition,cfg)` = capped match multiplier, EXACTLY 1.0 at neutral (Form=50/Morale=50/Fitness=100), clamped to [PerformanceFloorPercent/100, PerformanceCapPercent/100]=[0.70,1.08]; terms: form ±FormSwingPermille, morale ±MoraleSwingPermille, fitness −FitnessSwingPermille (fitness only ever reduces, tops at 100=no effect). `ApplyMatchResult(condition,minutes,TeamResult,rng,cfg)` (fitness drain ∝ minutes; form = bounded mean-reverting walk + result nudge if featured; morale += play/bench ± win/loss). `ApplyRest(condition,days,cfg)` (fitness recovery capped 100; morale decays toward neutral, never overshoots). `TeamResult{Loss,Draw,Win}`. BalanceConfig +ConditionBalance section (PerformanceFloor 70/Cap 108; Form/Morale/FitnessSwingPermille 60/50/200; FitnessDrainPer90 24/RecoveryPerDay 3; Form FormReversionPermille 350/RandomStep 5/ResultNudge 3/Neutral 50; Morale Neutral 50/Play 2/Bench 3/Win 4/Loss 4/Decay 1). FEEDING: TeamRatings.FromWithCondition(lineup,cfg) scales each player's rating by the multiplier; MatchEngine ctor gained `bool applyCondition=false` — DEFAULT OFF = byte-identical (golden masters/replays safe, no Version bump), and a neutral squad with the flag ON is ALSO identity (proven by test). Tests: Condition/ConditionTests.cs (10) — neutral=1.0, floor never <70% (20k sweep + worst 0/0/0=0.70), flag-off ignores condition, neutral-squad+flag-on == base engine, tired squad scores less, fitness drain/recovery, morale play/result/decay, **form 3-season mean-reversion (300 players ×114 matches, longest run below 35 ≤6)**, **rotation vs fixed XI over 6 blocks × 16 congested matches (gap 1d), aggregated points: fixed XI craters in fitness & wins fewer pts, rotation stays fresher & wins more** (form/morale pinned neutral in this harness to isolate fatigue). 111 green (101+10); existing 101 untouched (flag defaults off). DESIGN NOTES for review: (a) form params lean gentle (anti-frustration) → form std ~5 around 50, dips below 35 rare/brief; (b) fitness recovery 3/day is calibrated so a weekly cycle of full matches slowly accrues fatigue (rotation matters even at normal cadence); (c) chemistry term in morale is deferred (morale = playing-time+results+decay for now; chemistry/team-talks land with 4.5 support actions); (d) "tired player → sub" pre-match trigger (deferred from 3.5) NOT wired yet — needs a fitness gate on MatchRule; flag if you want it folded in here. SeasonProgressor/client wiring of condition (per-day evolution + opt-in engine flag) NOT done yet — Sim.Core-only task, like 3.2 before 3.3. New .cs files need Unity .meta on first import; build-simcore needed (shared/ changed).
- Match engine calibration (accepted by user): GoalCoefficient 0.92 → avg 2.44 goals/match, draws ~25%, team scoring 3+ in ~24% of matches, strong-vs-weak 82%, home advantage +9pp on home wins.
- Known quirks: generated defense ratings sit slightly above attack ratings (role-weight inflation) — GoalCoefficient compensates; extra per-match variance will come from form/fitness in 4.1.
