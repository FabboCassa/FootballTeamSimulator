# CLAUDE.md — session context for Football Team Simulator

Read ARCHITECTURE.md (design) and ROADMAP.md (plan + current status via checkboxes) before doing anything.

## Working agreement (do not violate)
- **NEVER run `git commit` / `git add` / any stateful git command** — the user commits from his Windows machine. Concurrent git access through the synced folder corrupts the index. Touch source files only.
- One roadmap task at a time. The user tests every task on his machine (`dotnet test`, Unity) before it is marked `[x]` in ROADMAP.md. Mark `[~]` while in progress.
- The sandbox has NO dotnet SDK and cannot reach Microsoft/dot.net domains — code cannot be compiled here. Write conservative, standard C# and let the user build. If a test might be statistically fragile, say so and ask for the output.
- Balance-sensitive changes go through the 1,000-match harness; the user pastes the printed distribution and we judge together before closing the task.
- Language: chat in Italian, all code/comments/docs in English.

## Environment facts
- User machine: Windows, .NET 10 SDK, Unity 6.3 LTS (6000.3.17f1), project folder `C:\Users\Fabbo\FootballTeamSimulator`.
- Unity project = `client/` subfolder (opened via Unity Hub); git repo = monorepo root, single GitHub repo for everything.
- After ANY change in `shared/`, the user must run `.\tools\build-simcore.ps1` so Unity gets fresh DLLs (Sim.Core.dll + Fts.Contracts.dll → `client/Assets/Plugins/SimCore/`, gitignored, .meta committed).
- Test command: `dotnet test` (all) or with harness output:
  `dotnet test shared/Sim.Core.Tests/Sim.Core.Tests.csproj --logger "console;verbosity=detailed"`
- Current test count: 124 green (4.3); 4.4 adds 6 → expect 130 once the user builds.

## Architecture quick reference
- `shared/Sim.Core` (netstandard2.1, no Unity deps, no I/O, deterministic): ALL game rules. Namespaces: Domain, Generation, Match, Config, Random (+ empty: Market, Development, Condition, Tactics, Career).
- Determinism rules: only `Pcg32`/`IRandomSource` for randomness; no `Math.Pow/Exp/Log/Sin` (use IntPow pattern); no DateTime.Now; integer math where possible. This enables replays = (seed + inputs) and the IL2CPP cross-check (task 1.6).
- Every tunable lives in `Config/BalanceConfig.cs` (sections: Generation, Match). No magic numbers in systems.
- `server/` = ASP.NET Core skeleton (Api/Application/Infrastructure) with /health; docker-compose (postgres 17, redis 8). Real backend work starts Phase 7.
- Unity client: MVP + VContainer + UniTask + UI Toolkit, asmdefs FTS.App/Services/Presenters/Views/MatchView. Only Boot scene exists so far.
- Localization: ALL user-facing strings go through ILocalizationService (tables in client/Assets/Resources/Localization/{en,it}.json, flat key->text). Views can't reference Services, so they receive a Func<string,string> translate delegate from presenters. Missing keys fall back to en, then render the key itself. NEVER hardcode UI strings in views/presenters. Dev console logs stay English.

## Status (2026-06-19)
- Phase 0 done (0.5 CI: workflow exists, verified on first GitHub push).
- Phase 1 done. 2.1 done (app shell: MessageBroker, ScreenNavigator with per-screen child scopes + Reveal() hook, placeholder screens, AppLifetimeScope/GameSessionService/AppEntryPoint; views code-built, UXML arrives 2.4+; Boot scene has UIDocument + PanelSettings + AppLifetimeScope).
- 2.2 done (career setup & save: Newtonsoft via manifest; CareerState; CareerFactory; LocalJsonSaveRepository gzip+atomic+FNV-hash log in persistentDataPath/career.sav; CareerSetup screen; MainMenu Continue with friendly errors; user verified save/load/corruption).
- 2.3 done (calendar & advance day: Sim.Core Fixture/Season/LeagueTable/FixtureGenerator/SeasonProgressor, SeasonBalance config, save v2 + v1→v2 migration, LocalClock, Hub Advance Day + console table; 59 tests green, user verified).
- **2.4 squad screen IN PROGRESS**: code written — Sim.Core: Match/LineupPlan (serializable, From/Materialize/TryMaterialize), SeasonProgressor.AdvanceDay now takes optional per-club LineupPlan dict and returns List<MatchOutcome> (fixture+report; invalid plans fall back to BestEleven silently); 5 new/updated tests (expect 64 green). Client: CareerState.UserLineup (additive, still save v2, null = auto), LocalClock passes user plan + logs user match result/scorers/XI-source to console, SquadView+SquadScreenPresenter (slot list + roster, tap-slot-then-player with swap semantics, Auto Pick/Save; player *value* column deferred to 5.1, formations beyond 4-3-3 to 3.2). NEEDS: build-simcore.ps1 + dotnet test + Play-mode test. MatchEngine.Version = 2 since 1.5 (position stream; score/events per seed unchanged from v1).
- 2.4 done (squad screen: LineupPlan in Sim.Core, SeasonProgressor plans+MatchOutcome, SquadView/presenter, user XI used in sim; 64 tests green, user verified).
- Localization infra done early (pulled from 6.2): LocalizationService + en/it JSON tables, all existing UI strings migrated, language toggle in MainMenu (cycles en/it, persisted in PlayerPrefs, default from system language). User verified.
- 2.5 done (league screen: ScorerTally/Season.Scorers in Sim.Core, LeagueView 3 tabs table/fixtures/scorers; 65 tests green, user verified).
- 2.6 done (match day instant: UserMatchLog handoff, MatchResultScreen with event timeline, LineupStrength harness best 72 vs worst 16 pts; 66 tests green, user verified).
- 2.7 done (season rollover: two-division worlds with DivisionStrengthStep 14, SeasonRollover P/R 3 + ages+1 + new fixtures from Pcg32(seed ^ year*mix, 777+division), CareerState v3 Leagues + legacy "League" JSON adapter + v2→v3 migration generating div2, SeasonService/SeasonEndScreen, Hub Next Match/End Season; 72 tests green, user verified). CareerFactory consts Div2GenSequence 55 / FixtureSequence 777 / Div2FixtureSequence 778 / Div2FirstClubId 101 / Div2FirstPlayerId 5001 — NEVER change (migrations depend on them).
- **🏁 PHASE 2 COMPLETE — vertical slice: a full season is playable end-to-end. Phase-end audit passed (determinism greps clean, no hardcoded UI strings, en/it tables at parity incl. format placeholders, asmdef layering respected, no file >300 lines in Sim.Core, no magic numbers outside BalanceConfig).**
- 3.1 done (match renderer, client-only, NO Sim.Core change → 72 tests stay green, no build-simcore needed). MatchView/MatchRenderer (VisualElement, painter2D top-down pitch + interpolated tokens; playback in wall-clock time scaled by speed, BaseSecondsAt1x 180; trajectories pre-smoothed once with a centred moving average — PlayerSmoothingRadius 2 / BallSmoothingRadius 3 — to kill per-tick noise jitter; no per-frame allocs; events MinuteChanged/EventReached/Finished). Views/MatchWatchView (HUD score+clock, controls 1x/2x/4x/Skip/Continue, event toast). Presenters/MatchWatchScreenPresenter (user club teal vs opponent red, running score, Continue → Pop to Hub then Push MatchResult timeline). HubPresenter pushes MatchWatchScreenPresenter on user match. FTS.Presenters asmdef += FTS.MatchView. Loc keys match.clock/speed_1x/2x/4x/skip (en+it). ARCH §5.3/§5.4 updated (renderer = UI Toolkit screen in stack, not additive scene). User verified (watchable, runs). Known quirk: off-ball movement still looks wandery (stream is presentation-only geometry, not real steering) — deeper movement realism deferred to backlog.
- 3.2 done (Sim.Core tactics system, user verified 80 green): new Tactics namespace — Formation (6 shapes 4-3-3/4-4-2/3-5-2/4-2-3-1/5-3-2/3-4-3 as role lists; distinct def/mid/att buckets, wide mids = CM, wide forwards = W); TacticInstructions (Mentality/Pressing/Tempo/Width, neutral middle) + Tactic struct (value-equatable, keys familiarity); TacticModifiers.Compute → attack/mid/def multipliers (self-effects = symmetric trade-offs; counter-matrix = zero-sum-over-field patterns [press↔tempo, mentality↔tempo, cyclic width]; familiarity penalty on att+def); FamiliarityModel + TacticFamiliarityLog (in-memory, save wiring deferred to 3.3); MatchTactics/TacticContext. BalanceConfig.Tactics (magnitudes only; ±patterns are structural in code). MatchEngine.Simulate gained optional MatchTactics param — DEFAULT null = byte-identical to pre-3.2 (no Version bump; golden master + existing 72 tests unaffected; neutral full-familiarity tactic also = identity, proven by test). TeamRatings.WithMultipliers; LineupSelector.BestEleven(club, Formation) overload. Tests: TacticsTests (8) — identity/determinism, all formations valid, self-effects symmetric, counter sign + width zero-sum (deterministic, provable), familiarity-beats-fresh (match harness, safe), counter match harness (informational, ±band), no-dominant-tactic sweep (81 combos round-robin, win rate <56% target). 80 tests (72 + 8), all green on run 1 (familiarity 49 vs 24 ✅; no-dominant top 53.8% Def/High/Slow/Wide < 55% ✅). BUT counter edge fell short: fast-vs-press 49.2% of decided games (target 55-60%) — confirmed the possession-confound risk: pressing's +midfield and tempo's −midfield self-effects created a possession gap (squared by PossessionSharpness) that drowned the counter. FIX (run 2 pending): possession now moved ONLY by Width; Pressing & Tempo self-effects rerouted to attack/defense; press↔tempo counter made dual (fast side +attack through the press, presser −defense exposed); mentality↔tempo counter moved to defense. Counter sign test now checks Defense (presser) + Attack (fast side). Run 2: counter now 56.0% of decided games ✅ (in 55-60% target, CounterPressVsTempoPercent 6); no-dominant top win rate 53.8% (Def/High/Slow/Wide) < 55% ✅; familiarity unchanged ✅. One over-strict EXTRA assert tripped (worst points share 38.8% < my 0.40 floor — Att/High/Slow/Narrow, a legitimately bad tactic; the ✅ has no floor) → floor relaxed to 0.35 as a broken-tactic tripwire only. Mild accepted asymmetry: defensive tactics edge out attacking ones (defense ratings sit above attack — same known quirk GoalCoefficient compensates). Save wiring of the user's chosen tactic + per-club familiarity is deferred to 3.3 (TacticFamiliarityLog is in-memory for now). Build warning seen at run 2 (1 warning, non-blocking) — chase if it recurs.
- 3.3 done (tactics screen, user verified 83 green + Play-mode). Harness: attacking GF/GA 80/37 vs defensive 48/17 over a season (large, correctly-signed effect); neutral-full-fam = season identity; user tactic leaves AI matches byte-identical ✅. Sim.Core: TacticPlan (serializable formation+instructions, Key/ToTactic/FromTactic, mirrors LineupPlan); SeasonProgressor.AdvanceDay gained optional `IReadOnlyDictionary<int,TacticContext> tactics` (4th arg) — per-fixture BuildTactics returns null when neither side has an entry (AI-vs-AI byte-identical) else fills missing side with TacticContext.Neutral(FamiliarityMax)=identity. Client: CareerState +UserTactic (TacticPlan, null=neutral 4-3-3, no tactic passed to sim while null) +TacticFamiliarity (Dictionary<string,int> keyed by TacticPlan.Key); save v3→v4 (additive, no regen). LocalClock builds the user-club TacticContext from UserTactic+stored familiarity, passes it, and after a played user match accumulates familiarity via FamiliarityModel.Accumulate (uses default BalanceConfig().Tactics). TacticsView (formation cycle, 4 instruction cycle-buttons, familiarity %, read-only next-opponent panel name+venue+best-XI OVR + "tactics unknown") + TacticsScreenPresenter (works on a clone; Save persists UserTactic and, IF formation changed vs previously-saved formation, re-picks UserLineup=BestEleven(club,formation) so Squad screen + in-match shape match). Hub "Tactics" button (between Squad and League). Squad screen now formation-aware: BestEleven + Auto Pick use UserFormation()=UserTactic?.Formation ?? F433; squad.lineup_caption no longer hardcodes "(4-3-3)". Loc: hub.tactics + full tactics.* block (formation/instruction names, familiarity, opponent line) en+it at parity. Tests added: SeasonProgressorTacticsTests (3) — neutral-full-fam = season identity, user tactic leaves AI matches byte-identical + changes ≥1 own match, attacking-vs-defensive over a season scores+concedes more (harness w/ TestContext output, large effect size, full familiarity to isolate the trade-off). Expect 83 green (80+3); existing 80 pass null so unchanged. DESIGN NOTE for review: a freshly-picked tactic starts at familiarity 0 → −UnfamiliarPenaltyPercent (12%) on att+def until it accrues (+20/match, full after 5) — intended per 3.2 mechanic; flag if it feels punishing. New .cs files need Unity to generate .meta on first import.
- 3.4 done (in-match interaction, user verified: Sim.Core 91 green + Play-mode pause/sub/tactic re-sim). Sim.Core (tested): MatchInput/MatchPlan (kickoff input + scheduled per-minute changes); MatchEngine.Simulate(MatchPlan,rng) applies changes at minute boundaries — empty plan byte-identical to legacy (golden masters safe), prefix before a change reproduces exactly, only the remainder re-rolls, same plan replays identically; legacy Simulate(home,away,rng,tactics) delegates to a 1-input plan. Position stream uses the FINAL active lineups (cosmetic; result never affected). SeasonProgressor: public FixtureRng(worldSeed,fixtureId) (shared seed), RecordResult/RevertResult (commit/undo a report's scorers; revert removes zeroed tallies). Tests: MatchResimTests (5: empty=legacy hash, replay-identical, prefix-identical+diverges, attacking switch raises 2nd-half scoring [resim 151→187], substitution keeps prefix+valid) + SeasonProgressorResultTests (3: FixtureRng determinism, record↔revert, revert+record replace w/ tally invariant). Client (done, untested): MatchRenderer.SeekToMinute (resume from pause minute, skip already-shown toasts); UserMatchContext/Holder (Game scope) reconstructs the user match's kickoff MatchPlan EXACTLY as SeasonProgressor resolved it (same lineup fallback, tactics, FixtureRng) so the unchanged re-sim == committed result; LocalClock.BuildUserMatchContext fills it after the user match (famAtKickoff captured before familiarity accrues). MatchWatchView +Pause button; new InMatchPanel overlay (on-pitch/bench tap-to-sub like Squad, 4 instruction cycles like Tactics, subs-left, Apply/Resume). MatchWatchScreenPresenter rewritten: pause→stop renderer→panel; sub (tap pitch slot then bench player, max 5 subs, MaxSubstitutions client const) and/or change instructions (NOT formation in-match); Apply re-sims _plan.WithChange(shownMinute+1, input) via FixtureRng and rebuilds the renderer seeked to the pause minute; Continue commits ONLY if changed (RevertResult(baseline)+RecordResult(current)+update UserMatchLog+save) so an intervened result becomes official and the league table stays consistent. GameSessionService registers UserMatchContextHolder. Loc: match.pause + inmatch.* (en+it parity); reuses tactics.*/role.* for instruction/role labels. DESIGN NOTES: (a) in-match tactic familiarity is PER-TACTIC (FamFor): the kickoff tactic keeps its kickoff familiarity, any other tactic switched to mid-match uses the club's stored familiarity for that key (0 if never drilled) → a switch to a drilled alternative is safe, an improvised one costs −UnfamiliarPenalty, so a touchline change can help/hurt/do-nothing by habituation (user-requested); familiarity % shown in the in-match panel; (b) formation can't change mid-match (would need role reassignment), instructions only; (c) re-sim is full 90' each Apply (instant). FUTURE: physical condition/form/fitness will also weigh on this in Phase 4.1; possible backlog item = named tactic preset slots for quick offensive/defensive switching. New .cs files need Unity to generate .meta on first import.
- 3.5 done (pre-match plans, Sim.Core verified 101 green = 91 + 10; client wiring untested in Play-mode — inert until an editor exists, see note (c)). Harness: the 10-scenario ✅ fires exactly like a static change on every seed (minute-only 40/40; losing@1 17/40, winning@60 24/40, drawing@70 15/40, not-losing@80 23/40, not-winning@65 32/40, losing-by-2@1 8/40, winning-by-5@1 1/40 — all branches exercised); headless plan changed 15 of the club's fixtures, all other fixtures byte-identical, run deterministic; no calibration regression (2.44 g/m, golden masters intact, counter 56%, sweep top 53.8%). Scope agreed with user: Sim.Core foundation + headless execution + minimal save plumbing, NO editor screen yet; triggers are minute+scoreline only (fitness "tired→sub" hooks in at 4.1). Sim.Core: Match/MatchRule.cs — `ScoreSituation` (Always/Losing/Drawing/Winning/NotWinning/NotLosing, Margin≥1 on Losing/Winning), `Substitution` (OutPlayerId + already-resolved In Player, keeps engine Club-free), `RuleAction` (optional TacticInstructions+InstructionFamiliarity and/or Sub; IsEmpty), `MatchRule` (FromMinute/When/Margin/Action, side-agnostic, ConditionMet(own,opp)), `MatchRuleApplier.Apply(active,rule,isHome,famMax)` (pure: sub rebuilds the side's lineup keeping the out-slot role; instruction change rebuilds the side's TacticContext keeping formation, other side untouched). MatchEngine: new `Simulate(plan, homeRules, awayRules, rng)`; legacy `Simulate(plan,rng)` delegates with null,null. Rules evaluated at each minute boundary AFTER static plan.Changes, BEFORE the action draw, against score-so-far; fire-once via local bool[] (no hidden state → pure (plan,rules,seed)); a fired rule that yields an invalid input is skipped but marked spent. NO RNG consumed by evaluation → null/empty/never-fire = byte-identical (golden masters safe, no Version bump). Match/PrematchPlan.cs: serializable `PrematchRule`/`PrematchPlan` (ids+enums like LineupPlan/TacticPlan) + `Resolve(club, Func<TacticInstructions,int> familiarityFor, famMax)` → List<MatchRule> (drops empty actions + subs whose In player isn't in the squad). SeasonProgressor.AdvanceDay gained optional 6th arg `IReadOnlyDictionary<int,IReadOnlyList<MatchRule>> rules`; per fixture, a side with rules routes through the rules overload, neither-side-has-rules keeps the exact legacy call (AI-vs-AI byte-identical) — this IS the skipped/unwatched execution path. Client: CareerState +UserPlan (PrematchPlan, null/empty=none) save v4→v5 (additive, no migration); LocalClock.ResolveUserRules (familiarity per resulting formation+instructions via TacticPlan.Key) passes the user club's rules into AdvanceDay AND into BuildUserMatchContext; UserMatchContext +HomeRules/AwayRules (user side only) so the 3.4 watch re-sim reproduces the committed result; MatchWatchScreenPresenter.OnApply now re-sims via the rules overload (null,null when no plan = byte-identical to 3.4). Tests: PrematchPlanTests (8: null-rules=legacy, empty-lists=legacy, never-fire=baseline×50 seeds, replay-identical, prefix-preserved, **TenScenarios_FireExactlyLikeAStaticChange** = the 10-scenario ✅ proved by equivalence to a static MatchInputChange at the first gate-open minute, Resolve keeps-valid/drops-invalid, resolved-rule fires like static) + SeasonProgressorPlanTests (2: headless plan changes own matches + leaves others byte-identical over a season, headless run deterministic). DESIGN NOTES for review: (a) rules fire at most once (oscillation-free; author two rules for "attack when behind / defend when ahead"); (b) instruction-rule familiarity is host-resolved per tactic (default famMax in harness; client reads TacticFamiliarity, 0 if never drilled → −UnfamiliarPenalty); (c) NO plan editor UI yet — UserPlan is currently only settable in code/save, the screen is a later step; (d) the user-match watch path stays consistent because rules live on the user's side of UserMatchContext and the re-sim overload is byte-identical with no plan. New .cs files (MatchRule.cs, PrematchPlan.cs) need Unity to generate .meta on first import.
- **4.1 done (condition model, Sim.Core only — 111 green verified by user; SeasonProgressor/client wiring intentionally deferred, NOT yet done).** Verified harness numbers: form 3-season longest run below 35 = **4** matches (≤6 ✅); fitness→result fresh 517 vs exhausted 318 goals/400 matches ✅; rotation vs fixed XI = fixed 72 pts vs rotated 89 pts over 6×16 congested matches (clear ~24% edge, robust). Pre-existing calibration UNTOUCHED (golden hash 0xCDEA5A2F7B9E5CF6, 2.44 g/m, draws 24.8%, strong 82%, counter 56%, sweep top 53.8%) — proves the opt-in flag default-off is byte-identical. NOTE: first rotation harness (single 14-match block, gap 2d) passed by only 15-vs-14 pts (fragile coin-flip) → hardened to **6 blocks × 16 matches, gap 1 day (truly congested), aggregated points** so the sign is robust; fixedEndFitness sanity tightened to <60. New `Condition` namespace: `Condition/ConditionModel.cs` — pure/deterministic, integer math + one seeded RNG draw for the form walk, no transcendental fns. `PerformanceMultiplier(condition,cfg)` = capped match multiplier, EXACTLY 1.0 at neutral (Form=50/Morale=50/Fitness=100), clamped to [PerformanceFloorPercent/100, PerformanceCapPercent/100]=[0.70,1.08]; terms: form ±FormSwingPermille, morale ±MoraleSwingPermille, fitness −FitnessSwingPermille (fitness only ever reduces, tops at 100=no effect). `ApplyMatchResult(condition,minutes,TeamResult,rng,cfg)` (fitness drain ∝ minutes; form = bounded mean-reverting walk + result nudge if featured; morale += play/bench ± win/loss). `ApplyRest(condition,days,cfg)` (fitness recovery capped 100; morale decays toward neutral, never overshoots). `TeamResult{Loss,Draw,Win}`. BalanceConfig +ConditionBalance section (PerformanceFloor 70/Cap 108; Form/Morale/FitnessSwingPermille 60/50/200; FitnessDrainPer90 24/RecoveryPerDay 3; Form FormReversionPermille 350/RandomStep 5/ResultNudge 3/Neutral 50; Morale Neutral 50/Play 2/Bench 3/Win 4/Loss 4/Decay 1). FEEDING: TeamRatings.FromWithCondition(lineup,cfg) scales each player's rating by the multiplier; MatchEngine ctor gained `bool applyCondition=false` — DEFAULT OFF = byte-identical (golden masters/replays safe, no Version bump), and a neutral squad with the flag ON is ALSO identity (proven by test). Tests: Condition/ConditionTests.cs (10) — neutral=1.0, floor never <70% (20k sweep + worst 0/0/0=0.70), flag-off ignores condition, neutral-squad+flag-on == base engine, tired squad scores less, fitness drain/recovery, morale play/result/decay, **form 3-season mean-reversion (300 players ×114 matches, longest run below 35 ≤6)**, **rotation vs fixed XI over 6 blocks × 16 congested matches (gap 1d), aggregated points: fixed XI craters in fitness & wins fewer pts, rotation stays fresher & wins more** (form/morale pinned neutral in this harness to isolate fatigue). 111 green (101+10); existing 101 untouched (flag defaults off). DESIGN NOTES for review: (a) form params lean gentle (anti-frustration) → form std ~5 around 50, dips below 35 rare/brief; (b) fitness recovery 3/day is calibrated so a weekly cycle of full matches slowly accrues fatigue (rotation matters even at normal cadence); (c) chemistry term in morale is deferred (morale = playing-time+results+decay for now; chemistry/team-talks land with 4.5 support actions); (d) "tired player → sub" pre-match trigger (deferred from 3.5) NOT wired yet — needs a fitness gate on MatchRule; flag if you want it folded in here. SeasonProgressor/client wiring of condition (per-day evolution + opt-in engine flag) NOT done yet — Sim.Core-only task, like 3.2 before 3.3. New .cs files need Unity .meta on first import; build-simcore needed (shared/ changed).
- **4.2 DONE [x] (condition UI + condition made LIVE; user accepted balance, 118 green, golden master unchanged).** Scope decided with user (two questions up front): (1) turn the engine effect ON now (condition pesa sul risultato, validare con harness) — YES; (2) evolve condition for the WHOLE WORLD (not just the user club) — YES. So this deliberately drops the AI-vs-AI byte-identical guarantee for the *live* career (a chosen balance change); the opt-in identity is still proven structurally (see test #1). Sim.Core: `Condition/ConditionProgressor.cs` (new) — pure/deterministic whole-world daily evolution: clubs that played drain their kickoff XI 90' + step the whole squad via ApplyMatchResult, every idle club ApplyRest(1); per-club form RNG = `Pcg32(worldSeed ^ clubId*0x9E37…, day)` so it's order-independent and replay-safe (no transcendental fns, integer math). `SeasonProgressor` ctor gained `bool applyCondition=false` (builds the engine condition-aware) and a new public `EvolveCondition(leagues,season,dayOutcomes,worldSeed,lineupPlans)` that builds per-club Participation (StarterIds via the same ResolveLineup the match used + TeamResult from the score) and delegates to ConditionProgressor. **AdvanceDay stays byte-identical** (evolution is a separate call); existing 101 Sim.Core tests untouched. CRITICAL ORDERING for the watched user match (3.4): AdvanceDay simulates on the pre-match condition, THEN EvolveCondition mutates it — so by the time the user watches, the live players are already drained. Fix: LocalClock captures a **kickoff condition snapshot** (both squads, clones of Form/Morale/Fitness) into `UserMatchContext.KickoffCondition` *before* calling EvolveCondition; MatchWatchScreenPresenter's re-sim (`ResimWithKickoffCondition`) restores those values for the duration of the sim (finally-restores the live evolved values), and its engine is now `new MatchEngine(applyCondition:true)`. Without this the watch re-sim prefix would diverge from the committed result. Client: LocalClock `_progressor = new SeasonProgressor(applyCondition:true)` + calls EvolveCondition each day after the snapshot; UserMatchContext +KickoffCondition (NOT persisted, regenerable); CareerState UNCHANGED — PlayerCondition (Form/Morale/Fitness) was already serialized, so no save-version bump. NOTE (corrected after the test run): generation does NOT start players neutral — Form=50 but Morale∈[50,70] and Fitness∈[95,100] (GenerationBalance), so fresh squads carry a small net condition effect and the live career diverges from the pre-condition engine from **day 1** (the deliberate effect, not identity); old v5 saves likewise carry their generated condition and diverge immediately on load. The identity guarantee is purely structural: forced-neutral squad + flag on == flag off (test #1). UI (the actual 4.2 ✅): `Presenters/ConditionFormatter.cs` (ConditionDisplay.Build → form arrow ▲/▬/▼, morale face :)/:|/:(, fitness 0-100, and a tooltip that ALWAYS states the cause per band); SquadView rows rebuilt as clickable VisualElements with a condition strip (fitness bar painted green/amber/red + form arrow + morale face) and `row.tooltip` = the why-text; SquadScreenPresenter fills the new VM fields for slots + roster. Loc: full `condition.*` block (3 tooltip formats + fitness/form/morale cause phrases) en+it at parity (17 keys each). Glyphs (▲▼ ●-family, ASCII faces) are iconography, not translatable text. Tests added: `Career/SeasonProgressorConditionTests.cs` (4) — (1) engine-flag-on + **forced-neutral** squad == legacy byte-identical over a full season (opt-in safety; initially written assuming neutral generation, which was WRONG → now pins neutral explicitly), (2) a full LIVE season replays deterministically incl. final per-player condition, (3) lifecycle: starters drain on a matchday & recover on rest (prints the numbers), (4) **calibration harness**: full live season prints goals/match, draw%, home/away win%, scoreline histogram + asserts a loose band [2.0,3.0] g/m & [15,38]% draws. **Run 1: 114/115 green — only test #1 failed (fixture 60 score flip) because generation randomizes morale/fitness so fresh squads aren't neutral; fixed by pinning neutral. Run 2 (user): 115/115 green ✅.** BALANCE DECISION: user accepted the 2.55 g/m (vs 2.44 baseline) — a web search confirmed fatigue lowers shooting accuracy/speed and good condition lifts attacking output, so "manage condition well → score more" is realistic and aligned with the design pillar; the small +0.11 league-average uplift (partly the generated morale floor of 50) is accepted. Condition is now LIVE and accepted. HARNESS NUMBERS (run 1): `[condition-live calibration]` 380 matches **2.55 g/m**, draws **24.2%**, home wins **45.5%**, away **30.3%** vs pre-condition baseline 2.44 g/m / 24.8% / home 48.4% — i.e. +0.11 g/m and home edge softened ~3pp (more per-match variance lets favourites slip slightly). `[lifecycle]` starters 76.0 → 85.0 fitness (drain then recover). BALANCE VERDICT (for the user to accept or tune): condition is ~scale-invariant when both sides tire equally, so the shift is small and stays in the accepted band — I'd accept it, but the generated morale floor of 50 (only ever ≥ neutral) is a candidate to re-centre later if we want exact baseline parity. DESIGN NOTES for review: (a) subs' partial minutes not modelled — a starter is credited a full 90', the rest 0' (kickoff XI only); (b) an intervened (subbed) watch result is re-committed but condition was already evolved on the kickoff XI assumption (minor, documented); (c) AI clubs field BestEleven every match (no rotation yet) so their regulars fatigue across a season — harmless to calibration per (a)'s scale-invariance, but means AI doesn't *exploit* rotation (AI rotation is a later/market-phase concern); (d) condition shown on the Squad screen AND the in-match pause panel (user-requested) via a shared `Views/ConditionStrip` helper; the pause panel reads the KICKOFF snapshot (the model has no within-match fatigue curve, so the value a player took into the game is what matters for a sub) — the tactics opponent panel could show it later. HANDOFF: shared/ changed → run `.\tools\build-simcore.ps1`, then `dotnet test … --logger "console;verbosity=detailed"` and paste the `[condition-live calibration]` + `[lifecycle]` lines so we judge the band together; new .cs files (ConditionProgressor, ConditionFormatter, SeasonProgressorConditionTests) need Unity .meta on first import; then Play-mode check the Squad screen bars/tooltips + that benching a tired player recovers his bar. Keep 4.2 at [~] until verified.
- **4.2 REFINEMENT (user-requested, Sim.Core changed → NOT yet built/harnessed): uniform starting fitness + stamina-driven drain.** (1) Generation now starts EVERY player at full fitness (`AttributeScale.MaxCondition`) so the bars are equal at career start; the random fitness draw is KEPT and discarded so the generation RNG stream stays byte-identical (golden masters safe — they're flag-off so fitness value is irrelevant anyway). (2) `ConditionModel.ApplyMatchResult` gained an optional `int stamina = 50` (neutral pivot) and a `StaminaScaledDrain` helper: drain = baseDrain × (1000 + (StaminaNeutral − stamina) × StaminaDrainSwingPermille / StaminaNeutral) / 1000, clamped ≥0 — low stamina tires faster, high slower, stamina=50 = exact identity (so the 4.1 tests and any default caller stay byte-identical). BalanceConfig.Condition += `StaminaNeutral=50`, `StaminaDrainSwingPermille=400` (±40% at the stamina extremes). `ConditionProgressor.ApplyMatch` passes `player.Attributes.Stamina` (already a generated 1-100 attribute, varies by role+noise). So differentiation now comes from how fast players tire, not from a random starting value — the user's intent. HANDOFF: build-simcore + dotnet test, paste `[condition-live calibration]` + `[lifecycle]` again (avg stamina ~50 keeps avg drain ~24, so league calibration should barely move; StaminaDrainSwingPermille is the tuning knob). **WITHIN-MATCH FATIGUE (user chose "incide sul risultato", engine-affecting): built, 118/118 green, harness run done.** New engine flag `MatchEngine(…, bool applyMatchFatigue=false)` — separate from applyCondition so golden masters and the flag-off / neutral-condition identity are untouched. Model: ratings are now recomputed EVERY minute (was: only on input change; recompute is byte-identical when the flag is off — identical values, no RNG consumed — so existing goldens hold); when on, each side's ratings are scaled by a per-side `FatigueFactor(minute, avgStamina)` = tiredness building linearly to `MatchFatigueAt90Permille` (80=−8% for a neutral-stamina XI) by full time, stepped back by `HalfTimeRecoveryPermille` (20=+2%) after minute 45 (so the 2nd half restarts fresher than the close of the 1st — the user's ask), then scaled by the on-pitch XI's AVERAGE stamina ((2·StaminaNeutral−avgStamina)/StaminaNeutral: low-stamina XI fades up to ~2×, a 100-stamina XI doesn't fade). KEY: equal-stamina sides fade equally → scale-invariant → league goals/match barely move; the effect is RELATIVE — the fresher/higher-stamina side (or one that subs on fresh legs, which lifts avgStamina) gains the edge, especially late. Config: BalanceConfig.Condition += `MatchFatigueAt90Permille=80`, `HalfTimeRecoveryPermille=20`. Threaded through `SeasonProgressor(…, bool applyMatchFatigue=false)`; client LIVE on both fronts — `LocalClock` SeasonProgressor and `MatchWatchScreenPresenter` engine both `applyMatchFatigue:true`. Tests: `Condition/MatchFatigueTests.cs` (3) — flag-off == base engine byte-identical (proves the per-minute recompute is safe), determinism per seed, and the relative-edge harness (high-stamina 95 vs low-stamina 10, 400 seeds, fatigue-on net GF-GA > fatigue-off net; the stamina rating-confound cancels in the on−off delta). `SeasonProgressorConditionTests` live runs now pass `applyMatchFatigue:true` (calibration/determinism/lifecycle); the identity test keeps fatigue OFF. DESIGN LIMITATIONS (v1, documented): fatigue is per-SIDE on avg stamina, not per-player on-pitch minutes — so a fresh sub helps via the side average rather than being individually tracked (good enough; per-player entry-time tracking deferred). HARNESS RESULTS (118/118 green; golden master 0xCDEA…UNCHANGED ✓): `[match-fatigue]` high-stamina net GF-GA over 400 = off **481** → on **635** (+154, big & correct-signed ✓). `[lifecycle]` 81.1 → 90.1 (drain now ~19 not 24 because starters' avg stamina <50 lifts endurance vs the old flat 24; still drains+recovers ✓). `[condition-live calibration]` 380 matches **2.60 g/m**, draws **20.5%**, home 47.6%, away 31.8% — vs the pre-fatigue 4.2 number 2.55 g/m / 24.2% draws, and pre-condition baseline 2.44 / 24.8%. So within-match fatigue is roughly goals-neutral (+0.05 g/m) BUT drops DRAWS ~3.7pp (24.2→20.5): the fresher/higher-stamina side pulling away late makes games more decisive. 20.5% is still in-band but below the historic ~25% target. BALANCE DECISION (user): softened `MatchFatigueAt90Permille` **80 → 60** (HalfTimeRecoveryPermille kept 20). Re-run (118/118 green, golden master 0xCDEA…unchanged): `[condition-live calibration]` **2.59 g/m, draws 21.3%** (only +0.8pp vs the 80 run — the decisiveness is structural: within-match fatigue rewards the fresher/higher-stamina side, so as soon as stamina differs draws fall ~3pp regardless of magnitude; softening further recovers little for a weaker effect), home 47.4%, away 31.3%; `[match-fatigue]` off 481 → on **580** (still clearly correct-signed). RECOMMENDATION to user: accept 60/21.3% (realistic, effect meaningful). **ACCEPTED by user — 4.2 closed [x]** (user signed off on the balance; Play-mode of the Squad + pause-panel condition strips is the user's to spot-check as usual).
- **4.3 DONE [x] (training system — harness VERIFIED 124 green by user, golden master unchanged; user closed it on the ✅. Client Training-screen + live wiring WRITTEN, Play-mode spot-check still the user's as usual — client can't be built here).** HARNESS NUMBERS (user run, 124/124 green, golden master 0xCDEA…UNCHANGED ✓): `[training-diverge]` attacking shoot+drib **2828** vs defending **2669**; defending defend **1586** vs attacking **1501** (correct-signed, ~3-4 pts/player in the focused area over a season with +20 headroom & shared RNG); `[training-decline]` 21/22 at-potential players lost ground over 2 seasons, capped at −8; `[training-world]` AI club skill-total +150/season (Balanced default; youth grow, veterans decline → world not frozen, mildly net-positive). User VERDICT: gentle/realistic movement accepted as-is; `GrowthPerMillePerWeight` (130) is the knob if training should feel heavier; the −8 decline cap stays mild until the 4.4 age curve. Scope decided with user (3 questions up front): (1) Sim.Core + harness first, screen + live wiring after; (2) the user controls ONLY his own club's training, but the WHOLE WORLD's players still improve/decline (AI clubs train a Balanced default); (3) focuses are by attribute GROUP (not single attribute). New `Development` namespace (was empty): `Development/TrainingFocus.cs` — `TeamTrainingFocus {Balanced,Attacking,Defending,Physical,Technical,Tactical}` + `IndividualTrainingFocus {None,Attacking,Defending,Physical,Technical}` (stable numeric values = save keys). `Development/TrainingPlan.cs` — serializable (TeamFocus + Dictionary<int,IndividualTrainingFocus> IndividualFocuses, FocusFor(id), Balanced()), mirrors LineupPlan/TacticPlan; host owns persistence. `Development/TrainingModel.cs` — PURE/deterministic, integer math + exactly `PlayerAttributes.SkillCount`(=10) RNG draws per player per week (one per skill, BOTH regimes, so changing focus re-thresholds the SAME rolls → isolates the effect in A/B harnesses; no transcendental fns). Two private weight tables (TeamWeights[6,10], IndividualWeights[5,10], same skill order as PlayerRating); `CombinedWeights(team,indiv)` = team+individual per skill. `ApplyTrainingWeek(player,team,indiv,rng,cfg)`: gap = Potential − Overall; gap>0 → **Grow** (per-skill +1 with prob = weight·GrowthPerMillePerWeight/100 · headroom-scale, clamped to GrowthMaxPerMille; gate recomputes Overall per increment so growth STOPS EXACTLY AT Potential — strict ceiling, slows near it); gap≤0 → **Decline** (each skill −1 at DeclinePerMille, but only while Overall stays ≥ Potential−DeclineFloorPoints (capped, anti-frustration), and a skill whose combined weight ≥ DeclineProtectionWeightThreshold declines slower — drilling keeps it sharp). `TacticalFamiliarityGain(team,cfg)` = the "affects tactic familiarity" half: Tactical focus → TacticalFocusFamiliarityGainPerWeek, every other focus → 0 (host adds it to the stored level via FamiliarityModel). `Development/TrainingProgressor.cs` — whole-world weekly evolution (like ConditionProgressor): `EvolveWeek(leagues, plans, worldSeed, week)`; user club uses its plan, every other club uses TrainingPlan.Balanced (AI default); per-club RNG = `Pcg32(worldSeed ^ clubId·0x9E37…, week)` → order-independent, replay-safe. Domain: added a per-skill INDEXER `PlayerAttributes[int]` (+ const SkillCount) in canonical skill order (clamping setters) so systems iterate skills generically — reused by TrainingModel, handy for 4.4. BalanceConfig += `DevelopmentBalance Development` section (GrowthPerMillePerWeight 130 / GrowthMaxPerMille 250 / HeadroomScaleCap 10; DeclinePerMille 35 / DeclineProtectionWeightThreshold 55 / DeclineProtectionPercent 70 / DeclineFloorPoints 8; TacticalFocusFamiliarityGainPerWeek 8). NOTHING in the existing engine/SeasonProgressor calls training → existing 118 tests & golden master UNAFFECTED (opt-in by being called, like the condition progressor). Tests: `Development/TrainingTests.cs` (6) — (1) **THE ✅** `SameSeed_DifferentTraining_DivergesAfterASeason`: two identical generated squads (= same save), potential lifted +20 for headroom, same weekly RNG stream, Attacking vs Defending focus → asserts attacking squad ends sharper in shoot+drib and defending squad stronger in defend (prints `[training-diverge]`); (2) determinism per seed+plan; (3) `Growth_SaturatesAtPotential_NeverExceedsIt` (small headroom, 3 seasons hardest focus → Overall ≤ Potential always); (4) `AtPotential_DeclinesGently_ButNeverBelowTheFloor` (potentials pinned to current, Tactical/unprotected, 2 seasons → some decline, all within [Potential−8, Potential], prints `[training-decline]`); (5) `TacticalFocus_IsTheOnlyFocusThatGainsFamiliarity`; (6) `WholeWorld_Evolves_AndIsDeterministic_AiClubsTrainToo` (two identical worlds + same plans agree everywhere; an AI club with NO plan still evolved — world isn't frozen; prints `[training-world]`). Expect 124 green (118+6). DESIGN NOTES for review: (a) growth/decline magnitudes are first-pass tunables — the ✅ asserts a comparative inequality so it holds regardless of exact calibration, but eyeball the printed `[training-diverge]`/`[training-decline]`/`[training-world]` numbers and tell me if a season's movement feels right (too fast/slow); (b) the age-based growth CURVE, minutes/facilities/performance modifiers and the monthly cadence are explicitly 4.4 — 4.3 approximates "ageing" only via the existing Potential (young players have headroom → improve; players at potential → gentle capped decline), which is enough for a non-stagnant world now; (c) GK development under non-Balanced focuses is weak (focus vectors barely touch Goalkeeping) — Balanced (AI default) trains it, so AI GKs are fine; revisit if user GKs feel stuck; (d) tactic familiarity from Tactical focus is exposed as a per-week gain but its application to the user's stored familiarity is CLIENT wiring (follow-up step), not in the progressor. HANDOFF: shared/ changed → run `.\tools\build-simcore.ps1`, then `dotnet test shared/Sim.Core.Tests/Sim.Core.Tests.csproj --logger "console;verbosity=detailed"` and paste the `[training-diverge]`, `[training-decline]`, `[training-world]` lines so we judge the movement together; new .cs files (TrainingFocus, TrainingPlan, TrainingModel, TrainingProgressor, TrainingTests) need Unity .meta on first import. Keep 4.3 at [~] until verified; then the follow-up step is the Training screen + LocalClock weekly EvolveWeek + CareerState.UserTraining (save vN→vN+1 additive) + Tactical-focus familiarity accrual. **CLIENT WIRING DONE (written, Play-mode untested — client code can't be built here):** CareerState +UserTraining (TrainingPlan, null = balanced default) +LastTrainingWeek (int); save **v4→v5** (LocalJsonSaveRepository CurrentSaveVersion 4→5 — note: the repo was still on 4 because 3.5's UserPlan rode in additively without a bump; the new `if(SaveVersion<5)` migration seeds LastTrainingWeek = CurrentDay/DaysBetweenRounds so existing saves are NOT retroactively trained, then training begins next week boundary). LocalClock: `EvolveTrainingUpTo(CurrentDay+1)` called at the TOP of AdvanceDay (BEFORE the day's matches) so attributes stay stable through the match sim AND the 3.4 watched-match re-sim (the re-sim restores condition, not attributes, so a week boundary must not move attributes between commit and watch — and Next Match stops the day a user match is played, so no extra training intervenes); whole-world `TrainingProgressor.EvolveWeek` with plans={user club → UserTraining} (null → all clubs incl. user train Balanced); a Tactical team focus accrues the user's current-tactic familiarity per week (`TrainingModel.TacticalFamiliarityGain`, capped at FamiliarityMax) via AccrueTacticalFamiliarity. Training weeks align with the matchday calendar (period = SeasonBalance.DaysBetweenRounds = 7). New screen: `Views/TrainingView.cs` (dumb — strings+playerId only, NO Sim.Core ref: team-focus cycle button + per-focus hint + roster rows each with an individual-focus cycle button + Save/Back) and `Presenters/TrainingScreenPresenter.cs` (works on a clone of UserTraining; cycles TeamTrainingFocus 0..5 and IndividualTrainingFocus 0..4 per player, None removes the entry; Save persists + autosaves). Resolved via Push<T> (constructor deps already in scope — no DI edit needed). Hub: +"Training" button (HubView event + HubPresenter OnTraining) between Tactics and League. Loc: `hub.training` + full `training.*` block (title/header/team_caption/team_focus/team.{6 focuses}/team_desc.{6}/roster_caption/individual.{5}/save/status.saved) en+it at parity (verified via Read — the bash mount lags the file-API writes so JSON was validated through the Read tool, not bash). HANDOFF: shared/ changed earlier → `.\tools\build-simcore.ps1` already needed; the new client `.cs` files (TrainingView, TrainingScreenPresenter) need Unity `.meta` on first import. Play-mode check: open Training from the Hub, set a team focus + a couple individual focuses, Save; advance ~2-3 weeks and confirm on the Squad screen that attributes shift toward the focus; set Tactical focus and confirm the tactic familiarity % on the Tactics screen climbs week over week; reload a pre-4.3 save and confirm it loads (v4→v5 migration) without a sudden development jump. Keep 4.3 at [~] until the Play-mode pass; the harness ✅ is already green.
- **4.4 DONE [x] (development & ageing — harness VERIFIED & ACCEPTED by user, 130 green, golden master 0xCDEA5A2F7B9E5CF6 UNCHANGED; client wiring WRITTEN, Play-mode spot-check the user's as usual).** HARNESS NUMBERS (user run, 130/130 green): `[aging-minutes]` start 14086 → playing **16495 (+2409)** vs benched **14919 (+833)** over 10 seasons (youth who play grow ~2.9× the benched ✓); `[aging-decline]` **22/22** thirty-somethings declined, avg drop 27 / worst 32 overall pts (**~3/season**), floored at 55% of potential (gentle, no collapse ✓); `[aging-world]` AI club skill-total **+113/season** (youth grow, veterans decline → world not frozen ✓). User VERDICT: balance accepted as-is; knobs if ever retuned are `AgeDeclineBasePerMille` (45, lower = gentler) and `MinutesGrowthFloorPermille` (350, higher = less bench penalty). Scope decided with user (3 questions up front): (1) Sim.Core + harness first, client wiring after (like 4.1/4.3); (2) the age curve rides the EXISTING 4.3 WEEKLY tick (one cadence already wired in LocalClock) rather than a separate "monthly dev tick" — the ROADMAP/ARCH "monthly" note predates the 4.3 weekly choice; (3) include ALL modifiers now — age + minutes drive it, with facilities + performance as live parameters (neutral defaults until the host feeds real values). New files (all pure/deterministic, integer math, no transcendental fns, opt-in by being called → existing 124 tests + golden master 0xCDEA… UNAFFECTED, the match engine never references development): `Development/AgeCurve.cs` — position-dependent age→growth/decline (STRUCTURAL per-role peak/onset offsets, magnitudes in config): `GrowthFactorPermille(age,role,cfg)` = 1000 while ≤ GrowthYouthFullAge, tapering linearly to 0 at the role's peak age (base GrowthPeakAge 27 + offset: GK +5, CB +2, FB 0, DM +1, CM 0, AM −1, W −2, ST −1); `DeclineMultiplierPermille(age,role,cfg)` = 0 before the role's onset (base DeclineOnsetAge 30 + offset: GK +5, CB +3, FB +1, DM +2, CM +1, AM 0, W −2, ST −1), 1000 at onset, +AgeDeclineRampPerMillePerYear (120) per further year, capped AgeDeclineMaxMultiplierPermille (1800). `Development/DevelopmentContext.cs` — readonly struct {PlayingSharePercent, FacilityLevel, PerformanceRating} + `Neutral(cfg)` (full minutes / neutral facility / neutral performance → growth scaled by age alone; what AI clubs & un-tracked players get). `Development/DevelopmentModel.cs` — the 4.4 superset of TrainingModel; `ApplyDevelopmentWeek(player,team,indiv,ctx,rng,cfg)` draws exactly SkillCount RNG/week (stream-stable A/B) and picks a regime from age: declineMult>0 → **DeclineByAge** (gentle, faster with age, drilled skills protected, floored at AgeDeclineFloorPercentOfPotential 55% of potential = hard anti-collapse, REGARDLESS of headroom — an ageing player loses ground even if he never reached potential); else gap>0 → **Grow** (4.3 headroom/potential gate × AgeCurve growth × minutes × facility × performance factors, each 1/1000, clamped GrowthMaxPerMille); else → **DeclineMaintenance** (identical to 4.3 at-potential drift, capped at potential−DeclineFloorPoints). **CLEAN SUPERSET (proven by test):** at age ≤ GrowthYouthFullAge + Neutral context every factor = 1000 → Grow is byte-identical to TrainingModel.ApplyTrainingWeek (4.3). `Development/DevelopmentProgressor.cs` — whole-world weekly evolution (ageing twin of TrainingProgressor): `EvolveWeek(leagues, plans, contexts, worldSeed, week)`, user club uses its TrainingPlan + every other club Balanced default, per-player ctx from `contexts` (absent = Neutral), per-club RNG `Pcg32(worldSeed ^ clubId·0x9E37…, week)` → order-independent/replay-safe; opt-in (TrainingProgressor 4.3 left intact for back-compat). BalanceConfig.DevelopmentBalance += age-curve block (GrowthYouthFullAge 19 / GrowthPeakAge 27 / DeclineOnsetAge 30 / AgeDeclineBasePerMille 45 / AgeDeclineRampPerMillePerYear 120 / AgeDeclineMaxMultiplierPermille 1800 / AgeDeclineFloorPercentOfPotential 55) + modifier block (MinutesGrowthFloorPermille 350 / FacilityNeutralLevel 50 / FacilityGrowthSwingPermille 300 / PerformanceNeutralRating 50 / PerformanceGrowthSwingPermille 300). Tests: `Development/AgingTests.cs` (6) — (1) **THE ✅** `YouthWithMinutes_GrowMoreThanBenchedYouth_OverTenSeasons` (two identical age-18 squads +25 headroom, same RNG, share 100 vs 0, +1 age/season; played > benched > start, prints `[aging-minutes]`); (2) `Veterans_DeclineGently_AndNeverCollapse_OverTenSeasons` (age-33 peaked squad, 10 seasons; every player ≥ floor−2, most decline, prints `[aging-decline]` avg/worst per-season drop); (3) `AgeCurve_IsPositionDependent_AndMonotonic` (W declines before GK; growth full→0; decline rises with age & caps); (4) `FullYouth_NeutralModifiers_ReducesExactlyToTrainingModel` (the superset identity, byte-exact vs 4.3); (5) determinism per seed+context+plan; (6) `WholeWorld_Ages_AndIsDeterministic_AiClubsToo` (two worlds agree; AI club skill-total changes, prints `[aging-world]`). Expect 130 green (124+6). DESIGN NOTES for review: (a) magnitudes are first-pass — the ✅ asserts comparative inequalities so they hold regardless of exact calibration, but eyeball `[aging-minutes]`/`[aging-decline]`/`[aging-world]` and tell me if youth growth / thirty-something decline feels right (AgeDeclineBasePerMille + ramp are the knobs); (b) minutes & facilities & performance currently only modulate GROWTH (not the decline rate) — a deliberate v1 simplification; (c) GK growth under non-Balanced focuses stays weak (same 4.3 quirk); (d) the floor is % of potential so a pot-80 vet bottoms ~44 — gentle, never collapses. HANDOFF: shared/ changed → run `.\tools\build-simcore.ps1`, then `dotnet test shared/Sim.Core.Tests/Sim.Core.Tests.csproj --logger "console;verbosity=detailed"` and paste the `[aging-minutes]`, `[aging-decline]`, `[aging-world]` lines so we judge the movement together; new .cs files (AgeCurve, DevelopmentContext, DevelopmentModel, DevelopmentProgressor, AgingTests) need Unity .meta on first import. **CLIENT WIRING DONE (written, Play-mode untested — client can't be built here):** LocalClock now drives `DevelopmentProgressor.EvolveWeek` (was TrainingProgressor) on the same weekly tick, with a per-user-club-player `DevelopmentContext`: playing share = starts/matches since the last tick (youth who play grow faster; bench → MinutesGrowthFloorPermille), performance = the player's live `Condition.Form` (4.1) as proxy (neutral 50 = no effect), facility = neutral until 5.5. AI clubs absent from the contexts dict ⇒ neutral context (age-only growth), matching the accepted `[aging-world]` harness. Minutes are tracked in `RecordUserMatchStarts()` (credits the resolved kickoff XI a full match, bench 0 — same convention as the condition model) accumulated into CareerState `StartsSinceTraining` + `UserMatchesSinceTraining`, consumed and cleared each training tick. **CROSS-SEASON FIX (latent 4.3 bug):** LastTrainingWeek is season-local and the new season resets CurrentDay→1, so the `LastTrainingWeek < targetWeek` guard would never fire again after season 1 — development silently stopped. Fixed: `SeasonService.EndSeason` now resets `LastTrainingWeek=0` + clears the minutes window at rollover, and LocalClock decorrelates the per-week RNG across seasons via `rngWeek = Season.Year * SeasonWeekStride(100) + weekInSeason` (so season 2 week 1 ≠ season 1 week 1; fully deterministic per (seed, year, week)). Save **v5→v6** (LocalJsonSaveRepository CurrentSaveVersion 5→6; additive migration just ensures the StartsSinceTraining dict is non-null + resets the counter — old saves begin a fresh minutes window, NO retroactive minutes). TrainingProgressor (4.3) left intact in Sim.Core for back-compat; the engine never reads development so golden masters/replays unaffected. DESIGN NOTES: (a) facility/performance: performance is LIVE via form proxy now, facility waits for 5.5 (the model param is already there); (b) subs' partial minutes still not credited (kickoff XI = full match, like condition v1); (c) the watched-match re-sim (3.4) is unaffected — development runs at the TOP of AdvanceDay (before the day's matches) and only mutates attributes/the minutes accumulator, never condition, so the re-sim (which restores condition, not attributes) still reproduces the committed result. HANDOFF: shared/ changed → `.\tools\build-simcore.ps1` (already needed for the harness); new client files were only edits (no new .cs), so no new .meta. Play-mode check: advance ~2-3 weeks with a settled XI and confirm on the Squad screen that regular starters develop while a permanently-benched young player lags; advance into a SECOND season and confirm development keeps happening (the cross-season fix); reload a pre-4.4 (v5) save and confirm it loads (v5→v6) without a development jump. The harness ✅ is verified+accepted; only the client Play-mode pass remains, as usual.
- Match engine calibration (accepted by user): GoalCoefficient 0.92 → avg 2.44 goals/match, draws ~25%, team scoring 3+ in ~24% of matches, strong-vs-weak 82%, home advantage +9pp on home wins.
- Known quirks: generated defense ratings sit slightly above attack ratings (role-weight inflation) — GoalCoefficient compensates; extra per-match variance will come from form/fitness in 4.1.
